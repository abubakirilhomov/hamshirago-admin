[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && npm install -g http-server"

[deploy]
startCommand = "http-server dist -p ${PORT:-3000} -a 0.0.0.0"
restartPolicyType = "ON_FAILURE"
